apiVersion: apps/v1
kind: Deployment
metadata:
  name: kube-killer-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: kube-killer-operator
  template:
    metadata:
      labels:
        name: kube-killer-operator
    spec:
      serviceAccountName: kube-killer-operator
      containers:
        - name: kube-killer-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          command:
          - kube-killer-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "kube-killer-operator"
